---
const { title, url, description, pubDate, author, image } = Astro.props;
---

<li>
  <a href={url} title={title} class="group">
    <article class="flex-1 h-full flex flex-col border border-gray">
      <div class="block w-full">
        <img
          class="aspect-[384/246] object-cover bg-center h-full"
          src={image}
          alt={title}
        />
      </div>
      <div
        class="flex flex-col items-start justify-between flex-1 w-full border-t p-8 bg-white">
        <div class="w-full">
          <div>
            <p class="text-black">{title}</p>
          </div>
          <p class="mt-2 text-sm text-neutral-500 line-clamp-3">
            {description}
          </p>
        </div>
        <footer>
          <div class="inline-flex space-x-1 items-center mt-6">
            <p class="text-xs font-medium text-black">
              {author}
            </p>
            <span aria-hidden="true">&middot;</span>
            <div class="flex text-xs text-black">
              <time datetime="2020-03-16"> {pubDate}</time>
            </div>
          </div>
        </footer>
      </div>
    </article>
  </a>
</li>

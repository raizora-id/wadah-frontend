import { renderHook } from '@testing-library/react';
import { useRange } from './use-range';

describe('useRange', () => {
  it('should run without crashing', () => {
    // Lexical context and editor mocks needed for full test
    // Placeholder test
    expect(typeof useRange).toBe('function');
  });
});
